CREATE TABLE DESPESAS_DB.ORCAMENTO (

        ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
        TIPO_DESPESA_ID BIGINT NOT NULL,
        VALOR NUMERIC(10,2) NOT NULL,
        DATA_INICIAL TIMESTAMP NOT NULL,
        DATA_FINAL TIMESTAMP NOT NULL,
        PRIMARY KEY(ID)
);

ALTER TABLE DESPESAS_DB.ORCAMENTO ADD CONSTRAINT ORCAMENTO_TIPO_DESPESA FOREIGN KEY (TIPO_DESPESA_ID) REFERENCES DESPESAS_DB.TIPO_DESPESA(ID)
 ON DELETE NO ACTION ON UPDATE NO ACTION;
